"""
This file is no longer needed.

Users now connect their Gmail directly through Slack using the /connect-gmail command.
The OAuth flow is handled automatically by the built-in callback server.

To run the bot: python main.py
"""

print("Gmail setup is now handled through Slack.")
print("Start the bot with: python main.py")
print("Then use /connect-gmail in Slack to link your Gmail account.")
